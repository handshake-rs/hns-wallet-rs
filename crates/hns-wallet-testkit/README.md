# hns-wallet-testkit

`hns-wallet-testkit` provides deterministic, explicitly non-mainnet fixtures
for wallet, provider, marketplace, and settlement tests.

Fixtures are qualification aids and never constitute a production backend,
secret source, or release authorization. See the
[workspace repository](https://github.com/handshake-rs/hns-wallet-rs) for the
ecosystem qualification matrix.
