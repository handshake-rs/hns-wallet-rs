# hns-wallet-provider

`hns-wallet-provider` implements origin-, generation-, account-, and
permission-bound Handshake Provider API policy.

It does not install a browser adapter or authorize product exposure by itself.
See the [workspace repository](https://github.com/handshake-rs/hns-wallet-rs)
for the approval contract and current provider release gates.
